# new project
    sphinx-quickstart

# build project
    sphinx-build -b html sourcedir builddir
    sphinx-build -b html src build

# create HTMl
    make html

# create pdf with via LaTex
    make latexpdf
