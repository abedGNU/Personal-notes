# Compile
    Create a folder called build
    Enter the folder
    Run the command Cmake ..
    Then make


    mkdir build or mkdir -p build
    cd build
    cmake ..
    make // or cmake --build .


